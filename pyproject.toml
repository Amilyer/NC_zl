[tool.ruff]
# 1. 放宽行长限制到 120 (标准是 88，NX 开发建议 120 或更长)
line-length = 120

[tool.ruff.lint]
# 2. 忽略 E501 (行太长)，因为自动换行可能会把代码切得很丑
# 忽略 E722 (裸 except)，这个需要你以后有空手动改，不要自动动它
ignore = ["E501", "E722"]

# 3. 开启基本检查
select = ["E", "F"]